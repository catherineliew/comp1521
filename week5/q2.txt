if number starts with
0x    then it is hexadecimal   e.g. 0x123
0     then it is octal         e.g. 043
otherwise decimal
no binary constants

printf("%d == 0%o == 0x%x", 100, 100, 100)

will print 100 == 0144 == 0x64